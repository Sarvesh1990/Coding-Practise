#include <iostream>
#include <string>
#include <set>
using namespace std;

set<string> * Permut (String myStr, set<string> * mySet)
{
    int i=0;
    while(myStr[i]!="\0")
    {
        mySet=
    }
}

int main()
{
    string myStr = "Sarvesh";
    set<string> * mySet = new set<string>();
    mySet = Permut(myStr, mySet);
}
